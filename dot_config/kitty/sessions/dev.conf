# Create the tab and set it's title
new_tab ! -- Dev

# Set the layout for the first tab
layout tall:bias=70;full_size=1;mirrored=false

# Launch programs in windows (panes)
# Main editor pane (adjust command as needed: nvim, vim, code --wait, etc.)
launch fish -c 'nvim -u NORC -i NONE'

# Split vertically for git/utility pane
# launch lazygit

# Split vertically again for build/test pane
# launch --location=vsplit --cwd=current
